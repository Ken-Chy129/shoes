<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.ken.shoes.mapper.MustCrawlMapper">

    <insert id="insertIgnore">
        insert ignore into must_crawl(platform, model_no) VALUE (
            #{platform},
            #{modelNo}
        )
    </insert>

    <delete id="deleteByPlatform">
        delete from must_crawl
        where platform = #{platform}
    </delete>

    <select id="queryByPlatformList" resultType="java.lang.String">
        select model_no from must_crawl
        where platform = #{platform}
    </select>

    <select id="selectAllModelNos" resultType="java.lang.String">
        select model_no from must_crawl;
    </select>

    <select id="countByPlatform" resultType="java.lang.Long">
        select count(*) from must_crawl
        where platform = #{platform}
        <if test="modelNo != null and modelNo != ''">
            and model_no like concat('%', #{modelNo}, '%')
        </if>
    </select>

    <select id="pageByPlatform" resultType="cn.ken.shoes.model.entity.MustCrawlDO">
        select * from must_crawl
        where platform = #{platform}
        <if test="modelNo != null and modelNo != ''">
            and model_no like concat('%', #{modelNo}, '%')
        </if>
        order by model_no
        limit #{startIndex}, #{pageSize}
    </select>

    <delete id="deleteByPlatformAndModelNo">
        delete from must_crawl where platform = #{platform} and model_no = #{modelNo}
    </delete>
</mapper>
