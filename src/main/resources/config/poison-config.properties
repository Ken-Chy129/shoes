# Poison Platform Configuration
# Auto-generated configuration file

# API Mode (查询价格接口)
api.mode=1

# Price Limit (价格上限)
max.price=5000

# Stop Query Price (是否暂停查价)
stop.query.price=false

# Import DB Data on Startup (开启启动时自动同步DB数据)
open.import.db.data=true

# No Price Cache (开启无价货号缓存)
open.no.price.cache=true

# Minimum Profit (最小利润)
min.profit=-30

# Minimum 3.5 Profit (3.5最小利润)
min.three.profit=-30

# Open All 3.5 (开启全量货号3.5)
open.all.three.five=false

# Use V2 API
use.v2.api=false