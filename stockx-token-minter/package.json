{
  "name": "stockx-token-minter",
  "version": "0.1.0",
  "private": true,
  "description": "无头浏览器发 token 机：保活 StockX 登录态，定时静默签发 12h graphql token 并写回后端",
  "type": "commonjs",
  "scripts": {
    "login": "node login.js",
    "mint-once": "node index.js --once",
    "start": "node index.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "playwright": "^1.48.0"
  }
}
